# Your cloud image worker. The same service handles background removal and
# server-side formats such as HEIC/TIFF/BMP/GIF/ICO.
NEXT_PUBLIC_IMAGE_API_URL=https://your-image-worker.example.com

# Optional cloud media worker for links you own or are authorised to save.
NEXT_PUBLIC_MEDIA_API_URL=https://your-media-worker.example.com
